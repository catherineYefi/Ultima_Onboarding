import React, { useEffect, useState } from "react";

export default function Rules() {
  const [open, setOpen] = useState(false);

  // открыть попап по глобальному событию
  useEffect(() => {
    const handler = () => setOpen(true);
    window.addEventListener("openRules", handler);
    return () => window.removeEventListener("openRules", handler);
  }, []);

  return (
    <section id="rules" className="section container">
      <h2 className="section-title">Правила</h2>
      <p className="section-subtitle">
        Коротко — режим неизбежности и прозрачность. Подробно — нажми кнопку.
      </p>

      <button className="cta-button primary" onClick={() => setOpen(true)}>
        Правила Ultima-группы
      </button>

      {open && (
        <div className="modal-backdrop" onClick={() => setOpen(false)}>
          <div className="modal" onClick={(e) => e.stopPropagation()}>
            <div className="modal-header">
              <h3>Правила Ultima-группы</h3>
              <button className="modal-close" onClick={() => setOpen(false)}>
                ✕
              </button>
            </div>

            <div className="modal-body">
              <h4>🎯 Основные принципы</h4>
              <ul>
                <li><strong>Режим неизбежности</strong> — выполняем всё в срок, без оправданий.</li>
                <li><strong>Фокус на ROI</strong> — каждое действие ведёт к росту бизнеса, а не «активности ради активности».</li>
                <li><strong>Прозрачность</strong> — вся динамика фиксируется в воркбуке, CRM и дашборде.</li>
              </ul>

              <hr />

              <h4>📋 Дисциплина</h4>
              <ol>
                <li>
                  <strong>Присутствие</strong>
                  <ul>
                    <li>Участник обязан быть на каждой встрече десятки и на стратсессии.</li>
                    <li>Опоздания фиксируются, более 2 подряд = штраф в рейтинге.</li>
                  </ul>
                </li>
                <li>
                  <strong>Артефакты</strong>
                  <ul>
                    <li>Декларация, презентация, P&L weekly, CRM-воронка — обязательны к дедлайну.</li>
                    <li>Несданные материалы = штраф −10 баллов в рейтинге Ultima.</li>
                  </ul>
                </li>
                <li>
                  <strong>Коммуникации</strong>
                  <ul>
                    <li>Ответ в чатах Ultima ≤ 12 часов.</li>
                    <li>«Тишина» более суток = красный флаг для трекера.</li>
                  </ul>
                </li>
                <li>
                  <strong>Цена слова</strong>
                  <ul>
                    <li>Зафиксированная в декларации сумма (или другой ресурс) списывается при невыполнении обязательства.</li>
                  </ul>
                </li>
              </ol>

              <hr />

              <h4>👥 Роли</h4>
              <p>Схему ролей добавим как изображение по ссылке, когда будет финальный файл.</p>

              <hr />

              <h4>🗓 Оффлайн-слёты (рекомендательно, по желанию)</h4>
              <ul>
                <li>В сезоне по желанию могут быть оффлайн-встречи группы (помимо общей стратсессии и выпускного).</li>
                <li>Формат: 1 день, полное погружение, «режим закрытой комнаты».</li>
                <li>Цели: синхронизация, работа над системными узкими местами, личная рефлексия и доверие.</li>
                <li>Присутствие обязательно для всех участников группы.</li>
              </ul>
            </div>

            <div className="modal-footer">
              <button className="cta-button" onClick={() => setOpen(false)}>
                Понятно
              </button>
            </div>
          </div>
        </div>
      )}
    </section>
  );
}

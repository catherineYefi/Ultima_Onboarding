# 📋 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: ULTIMA 9.0 ONBOARDING PORTAL
## Редизайн навигации, информационной архитектуры и UX

---

## 🎯 КОНТЕКСТ И ЦЕЛИ

### Что это за продукт:
ULTIMA 9.0 — премиальная программа бизнес-трансформации длительностью 6 месяцев. Онбординг-портал помогает новым участникам быстро погрузиться в программу, понять структуру, подготовиться к старту и получить доступ ко всем материалам.

### Целевая аудитория:
- Предприниматели МСБ (малый и средний бизнес)
- Возраст: 30-50 лет
- Технически подкованные, но ценят простоту
- Заплатили 600,000-2,000,000₽ за участие
- Ожидают премиум-уровень сервиса

### Текущая проблема:
❌ Навигация перегружена (10+ кнопок в хедере)
❌ Информация разбросана по табам (6 табов)
❌ Нет понятного пути: "что делать сначала, что потом"
❌ Пользователь теряется и не знает, с чего начать
❌ Нет sidebar для быстрой навигации
❌ Нет кнопки "наверх"
❌ Нет прогресс-трекинга
❌ На мобилке всё ломается

### Цель редизайна:
✅ Создать guided tour (пошаговый путь пользователя)
✅ Упростить навигацию до 3-4 основных разделов
✅ Добавить sidebar с якорями для быстрого перехода
✅ Добавить прогресс-трекинг онбординга
✅ Сделать mobile-first дизайн
✅ Создать ощущение премиум-продукта

---

## 🗺️ ИНФОРМАЦИОННАЯ АРХИТЕКТУРА (НОВАЯ)

### СТАРАЯ СТРУКТУРА (УДАЛИТЬ):
```
❌ Табы: Глоссарий | Введение | Roadmap | Старт | Подготовка | Цикл
❌ Навбар: 10+ кнопок
```

### НОВАЯ СТРУКТУРА:

```
┌─────────────────────────────────────────────────────────────┐
│ NAVBAR (sticky, minimal)                                    │
│ ----------------------------------------------------------- │
│ ULTIMA 9.0  |  [Онбординг]  [О программе]  [Документы]     │
│                                                     [☰ Меню]│
└─────────────────────────────────────────────────────────────┘

┌──────────────┬──────────────────────────────────────────────┐
│              │                                              │
│  SIDEBAR     │  MAIN CONTENT (one page scroll)              │
│  (якоря)     │                                              │
│              │  ┌────────────────────────────────────────┐  │
│  Онбординг:  │  │ HERO: Добро пожаловать в ULTIMA 9.0   │  │
│  ● Главная   │  └────────────────────────────────────────┘  │
│  ○ Глоссарий │                                              │
│  ○ Введение  │  ┌────────────────────────────────────────┐  │
│  ○ Roadmap   │  │ SECTION: Глоссарий терминов            │  │
│  ○ Checklist │  │ (ММ, Бадди, Ассистент, WIG...)         │  │
│  ○ Start-CC  │  └────────────────────────────────────────┘  │
│              │                                              │
│  Программа:  │  ┌────────────────────────────────────────┐  │
│  ○ О ULTIMA  │  │ SECTION: Знакомство с программой       │  │
│  ○ Ритм      │  │ (формула, цикл, роли)                  │  │
│  ○ Роли      │  └────────────────────────────────────────┘  │
│              │                                              │
│  Документы:  │  ┌────────────────────────────────────────┐  │
│  ○ NDA       │  │ SECTION: Дорожная карта сезона         │  │
│  ○ Презент.  │  │ (6 месяцев от Start-СС до Final-СС)    │  │
│  ○ Календарь │  └────────────────────────────────────────┘  │
│              │                                              │
│              │  ┌────────────────────────────────────────┐  │
│  [Прогресс]  │  │ SECTION: Стартовый чек-лист            │  │
│  ████░░░░    │  │ (8 пунктов с чекбоксами)               │  │
│  40%         │  └────────────────────────────────────────┘  │
│              │                                              │
│              │  ┌────────────────────────────────────────┐  │
│              │  │ SECTION: Подготовка к Start-СС         │  │
│              │  └────────────────────────────────────────┘  │
│              │                                              │
│              │  ┌────────────────────────────────────────┐  │
│              │  │ SECTION: Цикл работы (6 месяцев)       │  │
│              │  └────────────────────────────────────────┘  │
│              │                                              │
│              │  ┌────────────────────────────────────────┐  │
│              │  │ SECTION: Final-СС и завершение         │  │
│              │  └────────────────────────────────────────┘  │
│              │                                              │
└──────────────┴──────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ SCROLL TO TOP BUTTON (появляется при scroll > 500px)       │
│                                                   [↑ Наверх]│
└─────────────────────────────────────────────────────────────┘
```

### КЛЮЧЕВЫЕ ИЗМЕНЕНИЯ:

1. **Табы → One Page Scroll**
   - Все секции вертикально друг за другом
   - Sidebar показывает текущее положение
   - Плавный скролл между секциями

2. **10+ кнопок навбара → 3 основных раздела + меню**
   - Онбординг
   - О программе  
   - Документы
   - Остальное в бургер-меню

3. **Sidebar с якорями**
   - Всегда видно структуру
   - Клик = плавный переход
   - Текущий раздел подсвечен

4. **Прогресс-трекинг**
   - Показывает % прохождения онбординга
   - Обновляется по скроллу или чекбоксам

---

## 👤 USER JOURNEY (Путь пользователя)

### Правильный онбординг-флоу:

```
1. ПРИБЫТИЕ
   ↓
   Пользователь попадает на Hero
   Видит: "Добро пожаловать в ULTIMA 9.0"
   Действие: Читает краткое описание (2-3 строки)
   CTA: "Начать онбординг" или "Узнать больше"

2. ОРИЕНТАЦИЯ
   ↓
   Пользователь скроллит или кликает "Начать"
   Попадает в "Глоссарий терминов"
   Видит: ММ, Бадди, Ассистент, WIG и др.
   Действие: Читает определения (по 1-2 строки каждое)
   Прогресс: 10%

3. ПОНИМАНИЕ СТРУКТУРЫ
   ↓
   Скролл вниз → "Знакомство с программой"
   Видит: Формула (Трекер + Лидер + Группа + Ассистент)
   Видит: Цикл (6 месяцев, ритм встреч)
   Действие: Понимает, КАК устроена программа
   Прогресс: 25%

4. ВИДЕНИЕ ПУТИ
   ↓
   Скролл вниз → "Дорожная карта сезона"
   Видит: Timeline от Start-СС до Final-СС
   Видит: Ключевые точки (Start-СС, Месяцы 1-6, ММ, Final-СС)
   Действие: Понимает, ЧТО будет происходить
   Прогресс: 40%

5. ДЕЙСТВИЯ
   ↓
   Скролл вниз → "Стартовый чек-лист"
   Видит: 8 конкретных задач
   Действие: Начинает выполнять (подписать NDA, вступить в чат и т.д.)
   Прогресс: 40% → 60% (по мере выполнения)

6. ПОДГОТОВКА
   ↓
   Скролл вниз → "Подготовка к Start-СС"
   Видит: 3 встречи с БИ, Pre-Ultima Booster, AI-наставник
   Действие: Записывается на встречи
   Прогресс: 75%

7. ДЕТАЛИ
   ↓
   Скролл вниз → "Цикл работы"
   Видит: Ритм встреч, роли, ответственность
   Действие: Изучает детали процесса
   Прогресс: 90%

8. ЗАВЕРШЕНИЕ
   ↓
   Скролл вниз → "Final-СС и что дальше"
   Видит: Итоги, критерии зачета, следующие шаги
   Действие: Понимает финал
   Прогресс: 100%

9. НАВИГАЦИЯ
   ↓
   В любой момент:
   - Sidebar показывает, где он сейчас
   - Может перейти в любой раздел по клику
   - Может вернуться на главную
   - Может открыть документы
   - Может найти помощь
```

### КЛЮЧЕВОЙ ПРИНЦИП:
**Progressive Disclosure** — информация раскрывается постепенно, логично, без перегрузки.

---

## 🧩 КОМПОНЕНТЫ И СПЕЦИФИКАЦИИ

### 1. NAVBAR (Header)

**Расположение:** Вверху страницы, sticky (прилипает при скролле)

**Структура:**
```
┌─────────────────────────────────────────────────────────────┐
│ ULTIMA 9.0     [Онбординг] [О программе] [Документы]  [☰]   │
└─────────────────────────────────────────────────────────────┘
```

**Спецификация:**

```jsx
// Navbar.js
import React, { useState, useEffect } from "react";
import { Menu, X } from "lucide-react";

export default function Navbar({ sections, activeSection, scrollToSection }) {
  const [isScrolled, setIsScrolled] = useState(false);
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);

  useEffect(() => {
    const handleScroll = () => {
      setIsScrolled(window.scrollY > 50);
    };
    window.addEventListener("scroll", handleScroll);
    return () => window.removeEventListener("scroll", handleScroll);
  }, []);

  const mainSections = [
    { id: "onboarding", label: "Онбординг" },
    { id: "about", label: "О программе" },
    { id: "documents", label: "Документы" },
  ];

  return (
    <nav className={`navbar ${isScrolled ? "navbar-scrolled" : ""}`}>
      <div className="navbar-container">
        {/* Logo */}
        <div className="navbar-logo" onClick={() => scrollToSection("hero")}>
          ULTIMA 9.0
        </div>

        {/* Desktop Menu */}
        <ul className="navbar-menu">
          {mainSections.map((section) => (
            <li key={section.id}>
              <button
                className={`navbar-link ${
                  activeSection === section.id ? "active" : ""
                }`}
                onClick={() => scrollToSection(section.id)}
              >
                {section.label}
              </button>
            </li>
          ))}
        </ul>

        {/* Mobile Menu Toggle */}
        <button
          className="navbar-mobile-toggle"
          onClick={() => setIsMobileMenuOpen(!isMobileMenuOpen)}
        >
          {isMobileMenuOpen ? <X size={24} /> : <Menu size={24} />}
        </button>
      </div>

      {/* Mobile Dropdown Menu */}
      {isMobileMenuOpen && (
        <div className="navbar-mobile-menu">
          {mainSections.map((section) => (
            <button
              key={section.id}
              className="navbar-mobile-link"
              onClick={() => {
                scrollToSection(section.id);
                setIsMobileMenuOpen(false);
              }}
            >
              {section.label}
            </button>
          ))}
        </div>
      )}
    </nav>
  );
}
```

**CSS:**
```css
/* Navbar Styles */
.navbar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1100;
  
  background: rgba(10, 14, 39, 0.8);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid rgba(156, 163, 175, 0.1);
  
  transition: all 0.3s ease;
}

.navbar-scrolled {
  background: rgba(10, 14, 39, 0.95);
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.navbar-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  max-width: 1400px;
  margin: 0 auto;
  padding: 0 2rem;
  height: 72px;
}

.navbar-logo {
  font-size: 1.25rem;
  font-weight: 700;
  color: #E8EAED;
  cursor: pointer;
  transition: color 0.2s;
}

.navbar-logo:hover {
  color: #667eea;
}

.navbar-menu {
  display: flex;
  gap: 0.5rem;
  list-style: none;
  margin: 0;
  padding: 0;
}

.navbar-link {
  padding: 0.5rem 1rem;
  font-size: 0.875rem;
  font-weight: 500;
  color: #9CA3AF;
  background: transparent;
  border: none;
  border-radius: 0.5rem;
  cursor: pointer;
  transition: all 0.2s;
}

.navbar-link:hover {
  color: #E8EAED;
  background: rgba(102, 126, 234, 0.1);
}

.navbar-link.active {
  color: #667eea;
  background: rgba(102, 126, 234, 0.15);
}

.navbar-mobile-toggle {
  display: none;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  background: transparent;
  border: none;
  color: #E8EAED;
  cursor: pointer;
  border-radius: 0.5rem;
  transition: background 0.2s;
}

.navbar-mobile-toggle:hover {
  background: rgba(102, 126, 234, 0.1);
}

.navbar-mobile-menu {
  display: none;
}

/* Mobile Styles */
@media (max-width: 768px) {
  .navbar-menu {
    display: none;
  }
  
  .navbar-mobile-toggle {
    display: flex;
  }
  
  .navbar-mobile-menu {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding: 1rem;
    background: rgba(10, 14, 39, 0.98);
    border-top: 1px solid rgba(156, 163, 175, 0.1);
  }
  
  .navbar-mobile-link {
    padding: 0.75rem 1rem;
    background: transparent;
    border: none;
    color: #9CA3AF;
    font-size: 1rem;
    text-align: left;
    border-radius: 0.5rem;
    cursor: pointer;
    transition: all 0.2s;
  }
  
  .navbar-mobile-link:hover {
    background: rgba(102, 126, 234, 0.1);
    color: #E8EAED;
  }
}
```

**Поведение:**
- При scroll > 50px: добавляется класс `navbar-scrolled` (темнее фон, тень)
- На мобилке: кнопки скрыты, показывается бургер-меню
- Клик на раздел → плавный скролл к секции
- Активный раздел подсвечивается

---

### 2. SIDEBAR (Боковая навигация с якорями)

**Расположение:** Слева (desktop), скрыт на мобилке

**Структура:**
```
┌────────────────┐
│ Онбординг:     │
│ ● Главная      │ ← активная секция (залита цветом)
│ ○ Глоссарий    │
│ ○ Введение     │
│ ○ Roadmap      │
│ ○ Checklist    │
│ ○ Start-CC     │
│                │
│ Программа:     │
│ ○ О ULTIMA     │
│ ○ Ритм работы  │
│ ○ Роли         │
│                │
│ Документы:     │
│ ○ NDA          │
│ ○ Презентация  │
│ ○ Календарь    │
│                │
│ ──────────     │
│ Прогресс:      │
│ ████████░░     │
│ 40%            │
└────────────────┘
```

**Спецификация:**

```jsx
// Sidebar.js
import React from "react";
import { Check } from "lucide-react";

export default function Sidebar({ sections, activeSection, scrollToSection, progress }) {
  const sectionGroups = [
    {
      title: "Онбординг",
      items: [
        { id: "hero", label: "Главная" },
        { id: "glossary", label: "Глоссарий" },
        { id: "intro", label: "Введение" },
        { id: "roadmap", label: "Roadmap" },
        { id: "checklist", label: "Checklist" },
        { id: "prep-ss", label: "Start-CC" },
      ],
    },
    {
      title: "Программа",
      items: [
        { id: "about", label: "О ULTIMA" },
        { id: "rhythm", label: "Ритм работы" },
        { id: "roles", label: "Роли" },
      ],
    },
    {
      title: "Документы",
      items: [
        { id: "documents-nda", label: "NDA" },
        { id: "documents-presentation", label: "Презентация" },
        { id: "documents-calendar", label: "Календарь" },
      ],
    },
  ];

  return (
    <aside className="sidebar">
      <div className="sidebar-content">
        {sectionGroups.map((group) => (
          <div key={group.title} className="sidebar-group">
            <div className="sidebar-group-title">{group.title}</div>
            <ul className="sidebar-list">
              {group.items.map((item) => {
                const isActive = activeSection === item.id;
                const isCompleted = sections[item.id]?.completed;
                
                return (
                  <li key={item.id}>
                    <button
                      className={`sidebar-link ${isActive ? "active" : ""}`}
                      onClick={() => scrollToSection(item.id)}
                    >
                      <span className="sidebar-link-icon">
                        {isCompleted ? (
                          <Check size={16} />
                        ) : (
                          <span className={`sidebar-dot ${isActive ? "active" : ""}`} />
                        )}
                      </span>
                      <span>{item.label}</span>
                    </button>
                  </li>
                );
              })}
            </ul>
          </div>
        ))}

        {/* Progress Section */}
        <div className="sidebar-progress">
          <div className="sidebar-progress-label">Прогресс:</div>
          <div className="sidebar-progress-bar">
            <div
              className="sidebar-progress-fill"
              style={{ width: `${progress}%` }}
            />
          </div>
          <div className="sidebar-progress-text">{progress}%</div>
        </div>
      </div>
    </aside>
  );
}
```

**CSS:**
```css
/* Sidebar Styles */
.sidebar {
  position: fixed;
  top: 72px; /* Высота navbar */
  left: 0;
  width: 280px;
  height: calc(100vh - 72px);
  
  background: rgba(17, 22, 50, 0.8);
  border-right: 1px solid rgba(156, 163, 175, 0.1);
  backdrop-filter: blur(12px);
  
  overflow-y: auto;
  z-index: 1000;
  
  transition: transform 0.3s ease;
}

.sidebar-content {
  padding: 2rem 1rem;
}

.sidebar-group {
  margin-bottom: 2rem;
}

.sidebar-group-title {
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: #6B7280;
  margin-bottom: 0.75rem;
  padding: 0 1rem;
}

.sidebar-list {
  list-style: none;
  margin: 0;
  padding: 0;
}

.sidebar-link {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  width: 100%;
  padding: 0.625rem 1rem;
  
  background: transparent;
  border: none;
  border-radius: 0.5rem;
  
  font-size: 0.875rem;
  color: #9CA3AF;
  text-align: left;
  cursor: pointer;
  
  transition: all 0.2s;
}

.sidebar-link:hover {
  background: rgba(102, 126, 234, 0.1);
  color: #E8EAED;
}

.sidebar-link.active {
  background: rgba(102, 126, 234, 0.15);
  color: #667eea;
  font-weight: 500;
}

.sidebar-link-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 16px;
  height: 16px;
  flex-shrink: 0;
}

.sidebar-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #4B5563;
  transition: all 0.2s;
}

.sidebar-dot.active {
  background: #667eea;
  box-shadow: 0 0 8px rgba(102, 126, 234, 0.5);
}

.sidebar-link.active .sidebar-dot {
  background: #667eea;
}

/* Progress Section */
.sidebar-progress {
  margin-top: 2rem;
  padding: 1rem;
  border-top: 1px solid rgba(156, 163, 175, 0.1);
}

.sidebar-progress-label {
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: #6B7280;
  margin-bottom: 0.5rem;
}

.sidebar-progress-bar {
  width: 100%;
  height: 8px;
  background: rgba(75, 85, 99, 0.5);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 0.5rem;
}

.sidebar-progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
  border-radius: 4px;
  transition: width 0.5s ease;
}

.sidebar-progress-text {
  font-size: 0.875rem;
  font-weight: 600;
  color: #667eea;
  text-align: center;
}

/* Mobile - Hide Sidebar */
@media (max-width: 1024px) {
  .sidebar {
    transform: translateX(-100%);
  }
  
  .sidebar.open {
    transform: translateX(0);
  }
}
```

**Поведение:**
- Sticky (остается на месте при скролле)
- Текущая секция подсвечивается
- Клик на якорь → плавный скролл
- Выполненные секции показывают галочку ✓
- Прогресс-бар обновляется автоматически
- На мобилке скрыт (или открывается через бургер)

---

### 3. SCROLL TO TOP BUTTON

**Расположение:** Правый нижний угол

**Поведение:**
- Появляется при scroll > 500px
- Плавная анимация появления
- Клик → плавный скролл вверх

**Спецификация:**

```jsx
// ScrollToTop.js
import React, { useState, useEffect } from "react";
import { ArrowUp } from "lucide-react";

export default function ScrollToTop() {
  const [isVisible, setIsVisible] = useState(false);

  useEffect(() => {
    const handleScroll = () => {
      setIsVisible(window.scrollY > 500);
    };

    window.addEventListener("scroll", handleScroll);
    return () => window.removeEventListener("scroll", handleScroll);
  }, []);

  const scrollToTop = () => {
    window.scrollTo({
      top: 0,
      behavior: "smooth",
    });
  };

  if (!isVisible) return null;

  return (
    <button className="scroll-to-top" onClick={scrollToTop}>
      <ArrowUp size={24} />
    </button>
  );
}
```

**CSS:**
```css
.scroll-to-top {
  position: fixed;
  bottom: 2rem;
  right: 2rem;
  z-index: 1000;
  
  display: flex;
  align-items: center;
  justify-content: center;
  
  width: 48px;
  height: 48px;
  
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border: none;
  border-radius: 50%;
  
  color: white;
  cursor: pointer;
  
  box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
  
  animation: fadeInUp 0.3s ease;
  transition: all 0.3s ease;
}

.scroll-to-top:hover {
  transform: translateY(-4px);
  box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Mobile */
@media (max-width: 768px) {
  .scroll-to-top {
    bottom: 1rem;
    right: 1rem;
    width: 44px;
    height: 44px;
  }
}
```

---

### 4. PROGRESS TRACKER

**Два варианта реализации:**

#### Вариант A: В Sidebar (рекомендуется)
Уже включен в Sidebar компонент (см. выше)

#### Вариант B: Sticky Top Bar
```jsx
// ProgressBar.js
import React from "react";

export default function ProgressBar({ progress }) {
  return (
    <div className="progress-bar-container">
      <div className="progress-bar-fill" style={{ width: `${progress}%` }} />
    </div>
  );
}
```

**CSS:**
```css
.progress-bar-container {
  position: fixed;
  top: 72px; /* После navbar */
  left: 0;
  right: 0;
  height: 4px;
  background: rgba(75, 85, 99, 0.3);
  z-index: 1050;
}

.progress-bar-fill {
  height: 100%;
  background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
  transition: width 0.3s ease;
}
```

---

### 5. MAIN CONTENT LAYOUT

**Структура:**
```jsx
// App.js или MainLayout.js
import React, { useState, useEffect } from "react";
import Navbar from "./components/Navbar";
import Sidebar from "./components/Sidebar";
import ScrollToTop from "./components/ScrollToTop";
import Hero from "./components/Hero";
import Glossary from "./components/Glossary";
import Intro from "./components/Intro";
// ... другие секции

export default function App() {
  const [activeSection, setActiveSection] = useState("hero");
  const [progress, setProgress] = useState(0);

  // Smooth scroll to section
  const scrollToSection = (sectionId) => {
    const element = document.getElementById(sectionId);
    if (element) {
      const offset = 72; // Высота navbar
      const elementPosition = element.getBoundingClientRect().top;
      const offsetPosition = elementPosition + window.pageYOffset - offset;

      window.scrollTo({
        top: offsetPosition,
        behavior: "smooth",
      });
    }
  };

  // Track active section on scroll
  useEffect(() => {
    const handleScroll = () => {
      const sections = [
        "hero",
        "glossary",
        "intro",
        "roadmap",
        "checklist",
        "prep-ss",
        "about",
        "rhythm",
        "roles",
        "cycle",
        "final",
      ];

      let currentSection = "hero";
      
      for (const sectionId of sections) {
        const element = document.getElementById(sectionId);
        if (element) {
          const rect = element.getBoundingClientRect();
          if (rect.top <= 150 && rect.bottom >= 150) {
            currentSection = sectionId;
            break;
          }
        }
      }

      setActiveSection(currentSection);

      // Update progress (0-100%)
      const windowHeight = window.innerHeight;
      const documentHeight = document.documentElement.scrollHeight;
      const scrollTop = window.pageYOffset;
      const scrollPercent = (scrollTop / (documentHeight - windowHeight)) * 100;
      setProgress(Math.min(Math.round(scrollPercent), 100));
    };

    window.addEventListener("scroll", handleScroll);
    handleScroll(); // Initial call

    return () => window.removeEventListener("scroll", handleScroll);
  }, []);

  return (
    <div className="app">
      {/* Navbar */}
      <Navbar
        activeSection={activeSection}
        scrollToSection={scrollToSection}
      />

      {/* Sidebar */}
      <Sidebar
        sections={{}}
        activeSection={activeSection}
        scrollToSection={scrollToSection}
        progress={progress}
      />

      {/* Main Content */}
      <main className="main-content">
        <Hero id="hero" scrollToSection={scrollToSection} />
        <Glossary id="glossary" />
        <Intro id="intro" />
        <Roadmap id="roadmap" />
        <Checklist id="checklist" />
        <PrepToSS id="prep-ss" />
        <About id="about" />
        <Rhythm id="rhythm" />
        <Roles id="roles" />
        <Cycle id="cycle" />
        <Final id="final" />
      </main>

      {/* Scroll to Top */}
      <ScrollToTop />
    </div>
  );
}
```

**CSS:**
```css
.app {
  min-height: 100vh;
  background: #0A0E27;
}

.main-content {
  margin-left: 280px; /* Ширина sidebar */
  padding-top: 72px; /* Высота navbar */
  min-height: 100vh;
}

/* Mobile */
@media (max-width: 1024px) {
  .main-content {
    margin-left: 0;
  }
}
```

---

## 📐 СЕКЦИИ КОНТЕНТА

### ОБЩИЕ ПРАВИЛА ДЛЯ ВСЕХ СЕКЦИЙ:

1. **Каждая секция = отдельный компонент**
2. **Каждая секция имеет `id`** для якорей
3. **Padding:** 80px сверху и снизу (desktop), 40px (mobile)
4. **Max-width контента:** 900px (узкий контент) или 1200px (широкий)
5. **Centered:** контент по центру

**Базовая структура секции:**
```jsx
export default function SectionName({ id }) {
  return (
    <section id={id} className="section">
      <div className="container">
        {/* Section Header */}
        <div className="section-header">
          <div className="section-badge">📚 Раздел</div>
          <h2 className="section-title">Заголовок секции</h2>
          <p className="section-subtitle">Краткое описание</p>
        </div>

        {/* Section Content */}
        <div className="section-content">
          {/* Контент секции */}
        </div>
      </div>
    </section>
  );
}
```

**CSS для секций:**
```css
.section {
  padding: 80px 0;
  position: relative;
}

.section-header {
  text-align: center;
  margin-bottom: 3rem;
}

.section-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 1rem;
  margin-bottom: 1rem;
  
  background: rgba(102, 126, 234, 0.1);
  border: 1px solid rgba(102, 126, 234, 0.2);
  border-radius: 9999px;
  
  font-size: 0.875rem;
  font-weight: 600;
  color: #667eea;
}

.section-title {
  font-size: 2.25rem;
  font-weight: 700;
  color: #E8EAED;
  margin-bottom: 1rem;
}

.section-subtitle {
  font-size: 1.125rem;
  color: #9CA3AF;
  max-width: 600px;
  margin: 0 auto;
}

@media (max-width: 768px) {
  .section {
    padding: 40px 0;
  }
  
  .section-title {
    font-size: 1.875rem;
  }
}
```

---

### СЕКЦИЯ 1: HERO

**Цель:** Приветствие, ориентация, CTA

```jsx
// Hero.js
import React from "react";
import { ArrowRight, PlayCircle } from "lucide-react";

export default function Hero({ id, scrollToSection }) {
  return (
    <section id={id} className="hero">
      <div className="container">
        <div className="hero-content">
          {/* Badge */}
          <div className="hero-badge fade-in">
            6 месяцев • 2 стратсессии • Еженедельные встречи • 3 мастермайнда в год
          </div>

          {/* Title */}
          <h1 className="hero-title fade-in delay-1">
            ULTIMA 9.0 —{" "}
            <span className="text-gradient">
              стратегический контур неизбежного результата
            </span>
          </h1>

          {/* Subtitle */}
          <p className="hero-subtitle fade-in delay-2">
            Не обучение. Не трекинг. Стратегический контур для достижения
            конкретного бизнес-результата через систему, дисциплину и
            ответственность.
          </p>

          {/* CTA Buttons */}
          <div className="hero-cta fade-in delay-3">
            <button
              className="btn btn-primary btn-lg"
              onClick={() => scrollToSection("glossary")}
            >
              Начать онбординг
              <ArrowRight size={20} />
            </button>

            <button
              className="btn btn-secondary btn-lg"
              onClick={() => scrollToSection("about")}
            >
              О программе
              <PlayCircle size={20} />
            </button>
          </div>
        </div>
      </div>
    </section>
  );
}
```

---

### СЕКЦИЯ 2: GLOSSARY (Глоссарий терминов)

**Цель:** Быстро объяснить ключевые термины (ММ, Бадди, WIG и т.д.)

```jsx
// Glossary.js
import React from "react";

export default function Glossary({ id }) {
  const terms = [
    {
      title: "ММ (Мастермайнд)",
      description:
        "Встреча с топ-экспертами для обмена опытом и управленческих инсайтов. 3 ММ в год только для участников ULTIMA.",
    },
    {
      title: "Бадди (Buddy)",
      description:
        "Партнёр внутри группы для взаимной поддержки и ответственности. Созвоны раз в 2 недели.",
    },
    {
      title: "Ассистент группы",
      description:
        "Протоколирует встречи, контролирует сроки и фиксирует решения в течение всего сезона.",
    },
    {
      title: "WIG (Wildly Important Goal)",
      description:
        "Одна главная цель на 6 месяцев. Конкретная, измеримая, амбициозная, но достижимая.",
    },
    {
      title: "Start-СС",
      description:
        "Стратегическая сессия в начале (2 дня офлайн). Определение точки А, WIG, дорожной карты.",
    },
    {
      title: "Final-СС",
      description:
        "Стратегическая сессия в конце (на слёте). Подведение итогов, фиксация результатов, планирование следующего цикла.",
    },
  ];

  return (
    <section id={id} className="section">
      <div className="container container-narrow">
        {/* Header */}
        <div className="section-header">
          <div className="section-badge">📚 Глоссарий</div>
          <h2 className="section-title">Основные понятия ULTIMA</h2>
          <p className="section-subtitle">
            Ключевые термины, которые вы будете встречать в программе
          </p>
        </div>

        {/* Terms Grid */}
        <div className="glossary-grid">
          {terms.map((term, index) => (
            <div
              key={index}
              className="glossary-card fade-in"
              style={{ animationDelay: `${index * 0.1}s` }}
            >
              <h3 className="glossary-card-title">{term.title}</h3>
              <p className="glossary-card-description">{term.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

**CSS:**
```css
.glossary-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 1.5rem;
}

.glossary-card {
  padding: 1.5rem;
  background: rgba(30, 36, 64, 0.6);
  backdrop-filter: blur(12px);
  border: 1px solid rgba(102, 126, 234, 0.2);
  border-radius: 1rem;
  transition: all 0.3s;
}

.glossary-card:hover {
  border-color: rgba(102, 126, 234, 0.4);
  box-shadow: 0 0 30px rgba(102, 126, 234, 0.3);
  transform: translateY(-4px);
}

.glossary-card-title {
  font-size: 1.125rem;
  font-weight: 600;
  color: #667eea;
  margin-bottom: 0.5rem;
}

.glossary-card-description {
  font-size: 0.875rem;
  color: #9CA3AF;
  line-height: 1.6;
}
```

---

### СЕКЦИЯ 3: INTRO (Знакомство с программой)

**Цель:** Объяснить формулу, цикл, суть программы

```jsx
// Intro.js
import React from "react";

export default function Intro({ id }) {
  const formula = [
    {
      icon: "🎯",
      title: "Трекер",
      description:
        "Действующий предприниматель-практик. Управляет процессом, фокусирует на WIG, видит препятствия.",
    },
    {
      icon: "👥",
      title: "Лидер группы",
      description:
        "Капитан десятки: дисциплина, динамика, регулярность, удержание фокуса на ROI.",
    },
    {
      icon: "🤝",
      title: "Группа (8 человек)",
      description:
        "Адвайзери-борд друг для друга. Обратная связь, опыт, поддержка.",
    },
    {
      icon: "📋",
      title: "Ассистент",
      description:
        "Протоколирует встречи, контролирует сроки, ведёт дашборд группы.",
    },
  ];

  return (
    <section id={id} className="section">
      <div className="container">
        {/* Header */}
        <div className="section-header">
          <div className="section-badge">🧬 Знакомство</div>
          <h2 className="section-title">
            Знакомство с программой ULTIMA 9.0
          </h2>
          <p className="section-subtitle">
            ULTIMA 9.0 — это премиальный тариф в экосистеме Нечто для
            предпринимателей, которым актуально давление на выручку и конкретный
            результат за 6 месяцев. Это не обучение и не трекинг. Это
            стратегический контур роста через систему неизбежности результата.
          </p>
        </div>

        {/* Formula Section */}
        <div className="mb-12">
          <h3 className="text-center text-2xl font-bold mb-6 text-primary">
            Формула неизбежности результата
          </h3>
          <div className="grid grid-2 gap-6">
            {formula.map((item, index) => (
              <div
                key={index}
                className="card-gradient fade-in"
                style={{ animationDelay: `${index * 0.1}s` }}
              >
                <div className="text-4xl mb-3">{item.icon}</div>
                <h4 className="text-xl font-semibold mb-2">{item.title}</h4>
                <p className="text-secondary">{item.description}</p>
              </div>
            ))}
          </div>
        </div>

        {/* Cycle Info */}
        <div className="card-glass">
          <h3 className="text-xl font-bold mb-4">Цикл: 6 месяцев</h3>
          <ul className="list-check">
            <li>Start-СС (2 дня офлайн) → определение точки А и WIG</li>
            <li>
              Месяцы 1-6: встречи каждую неделю (после первого месяца — чередование
              трекер/лидер)
            </li>
            <li>Final-СС (на слёте) → подведение итогов, фиксация результатов</li>
          </ul>
        </div>
      </div>
    </section>
  );
}
```

---

### СЕКЦИЯ 4: ROADMAP (Дорожная карта сезона)

**Цель:** Визуализировать timeline от Start-СС до Final-СС

```jsx
// Roadmap.js
import React from "react";
import { Calendar, Target, Users, Award } from "lucide-react";

export default function Roadmap({ id }) {
  const timeline = [
    {
      icon: <Calendar size={24} />,
      title: "Подготовка к Start-СС",
      description: "3 встречи с БИ, сбор фактов и цифр, Pre-Ultima Booster, AI-наставник",
      duration: "2-3 недели",
    },
    {
      icon: <Target size={24} />,
      title: "Start-СС (офлайн)",
      description: "Определение точки А, WIG, приборов контроля, дорожной карты на 6 месяцев",
      duration: "2 дня",
    },
    {
      icon: <Users size={24} />,
      title: "Основной цикл",
      description:
        "Месяц 1: встречи каждую неделю. Далее — чередование трекер/лидер. Бадди-созвоны раз в 2 недели",
      duration: "6 месяцев",
    },
    {
      icon: <Award size={24} />,
      title: "Final-СС (на слёте)",
      description: "Подведение итогов, фиксация результатов, планирование следующего цикла",
      duration: "1 день",
    },
  ];

  return (
    <section id={id} className="section">
      <div className="container container-narrow">
        {/* Header */}
        <div className="section-header">
          <div className="section-badge">🗺️ Roadmap</div>
          <h2 className="section-title">Дорожная карта сезона</h2>
          <p className="section-subtitle">
            Путь от подготовки к Start-СС до Final-СС и результата
          </p>
        </div>

        {/* Timeline */}
        <div className="roadmap-timeline">
          {timeline.map((stage, index) => (
            <div
              key={index}
              className="roadmap-stage fade-in"
              style={{ animationDelay: `${index * 0.15}s` }}
            >
              <div className="roadmap-stage-icon">{stage.icon}</div>
              <div className="roadmap-stage-content">
                <div className="roadmap-stage-header">
                  <h3 className="roadmap-stage-title">{stage.title}</h3>
                  <span className="roadmap-stage-duration">{stage.duration}</span>
                </div>
                <p className="roadmap-stage-description">{stage.description}</p>
              </div>
              {index < timeline.length - 1 && (
                <div className="roadmap-connector" />
              )}
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

**CSS:**
```css
.roadmap-timeline {
  position: relative;
}

.roadmap-stage {
  position: relative;
  display: flex;
  gap: 1.5rem;
  padding: 1.5rem;
  margin-bottom: 1.5rem;
  
  background: rgba(30, 36, 64, 0.6);
  backdrop-filter: blur(12px);
  border: 1px solid rgba(102, 126, 234, 0.2);
  border-radius: 1rem;
  
  transition: all 0.3s;
}

.roadmap-stage:hover {
  border-color: rgba(102, 126, 234, 0.4);
  box-shadow: 0 0 30px rgba(102, 126, 234, 0.3);
  transform: translateX(8px);
}

.roadmap-stage-icon {
  flex-shrink: 0;
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
  
  background: rgba(102, 126, 234, 0.2);
  border-radius: 0.75rem;
  color: #667eea;
}

.roadmap-stage-content {
  flex: 1;
}

.roadmap-stage-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  margin-bottom: 0.5rem;
}

.roadmap-stage-title {
  font-size: 1.125rem;
  font-weight: 600;
  color: #E8EAED;
}

.roadmap-stage-duration {
  font-size: 0.75rem;
  font-weight: 600;
  padding: 0.25rem 0.75rem;
  background: rgba(102, 126, 234, 0.2);
  border-radius: 9999px;
  color: #667eea;
}

.roadmap-stage-description {
  font-size: 0.875rem;
  color: #9CA3AF;
  line-height: 1.6;
}

.roadmap-connector {
  position: absolute;
  left: 2.25rem;
  top: 100%;
  width: 2px;
  height: 1.5rem;
  background: rgba(102, 126, 234, 0.3);
}
```

---

### СЕКЦИЯ 5: CHECKLIST (Стартовый чек-лист)

**Цель:** Конкретные действия для участника

```jsx
// Checklist.js
import React, { useState, useEffect } from "react";
import { Check } from "lucide-react";

export default function Checklist({ id }) {
  const [checklist, setChecklist] = useState([
    { id: 1, text: "Подписал NDA", completed: false },
    { id: 2, text: "Вступил в чаты группы", completed: false },
    { id: 3, text: "Узнал про роль buddy (назначение на первой встрече)", completed: false },
    { id: 4, text: "Записал видео-визитку о бизнесе (2–3 минуты)", completed: false },
    { id: 5, text: 'Заполнил "Точку А и Б"', completed: false },
    { id: 6, text: "Подготовил презентацию для Start-СС", completed: false },
    { id: 7, text: "Составил черновик декларации WIG", completed: false },
    { id: 8, text: "Определил первую золотую задачу", completed: false },
  ]);

  // Load from localStorage
  useEffect(() => {
    const saved = localStorage.getItem("ultima-checklist");
    if (saved) {
      setChecklist(JSON.parse(saved));
    }
  }, []);

  // Save to localStorage
  const toggleItem = (itemId) => {
    const updated = checklist.map((item) =>
      item.id === itemId ? { ...item, completed: !item.completed } : item
    );
    setChecklist(updated);
    localStorage.setItem("ultima-checklist", JSON.stringify(updated));
  };

  const completedCount = checklist.filter((item) => item.completed).length;
  const totalCount = checklist.length;
  const progress = Math.round((completedCount / totalCount) * 100);

  return (
    <section id={id} className="section">
      <div className="container container-narrow">
        {/* Header */}
        <div className="section-header">
          <div className="section-badge">✅ Checklist</div>
          <h2 className="section-title">Стартовый чек-лист</h2>
          <p className="section-subtitle">
            Обязательные действия перед началом программы
          </p>
        </div>

        {/* Progress */}
        <div className="checklist-progress">
          <div className="checklist-progress-bar">
            <div
              className="checklist-progress-fill"
              style={{ width: `${progress}%` }}
            />
          </div>
          <div className="checklist-progress-text">
            {completedCount} из {totalCount} выполнено ({progress}%)
          </div>
        </div>

        {/* Checklist Items */}
        <div className="checklist-items">
          {checklist.map((item, index) => (
            <div
              key={item.id}
              className={`checklist-item ${item.completed ? "completed" : ""}`}
              onClick={() => toggleItem(item.id)}
              style={{ animationDelay: `${index * 0.05}s` }}
            >
              <div className="checklist-item-checkbox">
                {item.completed && <Check size={16} />}
              </div>
              <div className="checklist-item-text">{item.text}</div>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

**CSS:**
```css
.checklist-progress {
  margin-bottom: 2rem;
  padding: 1.5rem;
  background: rgba(30, 36, 64, 0.6);
  border-radius: 1rem;
  border: 1px solid rgba(102, 126, 234, 0.2);
}

.checklist-progress-bar {
  width: 100%;
  height: 12px;
  background: rgba(75, 85, 99, 0.5);
  border-radius: 6px;
  overflow: hidden;
  margin-bottom: 0.75rem;
}

.checklist-progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
  border-radius: 6px;
  transition: width 0.5s ease;
}

.checklist-progress-text {
  text-align: center;
  font-size: 0.875rem;
  font-weight: 600;
  color: #667eea;
}

.checklist-items {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.checklist-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 1rem 1.5rem;
  
  background: rgba(30, 36, 64, 0.6);
  border: 1px solid rgba(156, 163, 175, 0.2);
  border-radius: 0.75rem;
  
  cursor: pointer;
  transition: all 0.2s;
  
  animation: fadeInUp 0.3s ease forwards;
  opacity: 0;
}

.checklist-item:hover {
  border-color: rgba(102, 126, 234, 0.4);
  background: rgba(30, 36, 64, 0.8);
}

.checklist-item.completed {
  border-color: rgba(34, 197, 94, 0.3);
  background: rgba(34, 197, 94, 0.05);
}

.checklist-item-checkbox {
  flex-shrink: 0;
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  
  border: 2px solid rgba(156, 163, 175, 0.5);
  border-radius: 0.375rem;
  
  transition: all 0.2s;
}

.checklist-item.completed .checklist-item-checkbox {
  background: #22c55e;
  border-color: #22c55e;
  color: white;
}

.checklist-item-text {
  font-size: 1rem;
  color: #E8EAED;
}

.checklist-item.completed .checklist-item-text {
  text-decoration: line-through;
  opacity: 0.7;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

---

## 🎯 ПРИОРИТИЗАЦИЯ ЗАДАЧ

### 🔴 ФАЗА 1 - КРИТИЧНО (СДЕЛАТЬ В ПЕРВУЮ ОЧЕРЕДЬ)

**Время:** 4-6 часов

**Задачи:**

1. **Создать новый Navbar**
   - Компонент `Navbar.js`
   - 3 основных раздела + бургер-меню
   - Sticky behavior
   - Мобильная адаптация
   - **Критерий приемки:** Навбар работает, мобильное меню открывается, клик на раздел скроллит

2. **Создать Sidebar с якорями**
   - Компонент `Sidebar.js`
   - Список всех секций
   - Активная секция подсвечивается
   - Smooth scroll при клике
   - Прогресс-бар внизу
   - **Критерий приемки:** Sidebar видно, якоря работают, активная секция подсвечена

3. **Создать ScrollToTop кнопку**
   - Компонент `ScrollToTop.js`
   - Появляется при scroll > 500px
   - Smooth scroll to top
   - **Критерий приемки:** Кнопка появляется/исчезает, скролл наверх работает

4. **Убрать табы → сделать one page scroll**
   - Переработать `Onboarding.js`
   - Все секции вертикально
   - Каждая секция с `id`
   - **Критерий приемки:** Табов нет, все секции видны вертикально, якоря работают

5. **Настроить tracking активной секции**
   - В `App.js` добавить `useEffect` для отслеживания скролла
   - Обновлять `activeSection` при скролле
   - **Критерий приемки:** При скролле активная секция меняется в Sidebar

---

### 🟡 ФАЗА 2 - ВАЖНО (СДЕЛАТЬ ВТОРЫМ)

**Время:** 3-4 часа

**Задачи:**

6. **Переработать Hero секцию**
   - Добавить градиент на заголовок
   - Улучшить кнопки
   - Fade-in анимации
   - **Критерий приемки:** Hero выглядит премиум, градиент работает

7. **Создать Glossary секцию**
   - Компонент `Glossary.js`
   - Grid из карточек терминов
   - Hover эффекты
   - **Критерий приемки:** Термины в карточках, hover работает

8. **Создать Intro секцию**
   - Компонент `Intro.js`
   - Формула неизбежности (4 карточки)
   - Инфо о цикле
   - **Критерий приемки:** Формула отображена, цикл описан

9. **Создать Roadmap секцию**
   - Компонент `Roadmap.js`
   - Timeline с этапами
   - Иконки для каждого этапа
   - **Критерий приемки:** Timeline виден, этапы последовательны

10. **Создать Checklist секцию**
    - Компонент `Checklist.js`
    - 8 пунктов с чекбоксами
    - LocalStorage для сохранения
    - Прогресс-бар
    - **Критерий приемки:** Чекбоксы работают, прогресс обновляется, сохраняется в localStorage

---

### 🟢 ФАЗА 3 - ФИНАЛЬНЫЕ ШТРИХИ (ОПЦИОНАЛЬНО)

**Время:** 2-3 часа

**Задачи:**

11. **Breadcrumbs**
    - Компонент `Breadcrumbs.js`
    - Показывает путь: Главная > Онбординг > Раздел

12. **Search/Filter**
    - Поиск по секциям
    - Быстрая навигация

13. **Guided Tour**
    - Интеграция Intro.js или Shepherd.js
    - Пошаговый тур для новых пользователей

14. **Tooltips**
    - Подсказки для терминов
    - Используя библиотеку Tippy.js

---

## ✅ КРИТЕРИИ ПРИЕМКИ

### Desktop (> 1024px):

- [ ] Navbar sticky работает
- [ ] Sidebar видно слева
- [ ] Все якоря работают (smooth scroll)
- [ ] Активная секция подсвечена в Sidebar
- [ ] Кнопка "наверх" появляется при scroll > 500px
- [ ] Прогресс-бар обновляется при скролле
- [ ] Все секции последовательно (one page scroll)
- [ ] Hover эффекты на карточках работают
- [ ] Градиент на заголовке Hero виден
- [ ] Чек-лист сохраняется в localStorage

### Tablet (768px - 1024px):

- [ ] Sidebar скрыт или адаптирован
- [ ] Навбар адаптирован
- [ ] Контент читабелен

### Mobile (< 768px):

- [ ] Навбар показывает бургер-меню
- [ ] Sidebar полностью скрыт
- [ ] Все секции адаптированы
- [ ] Кнопки full-width
- [ ] Шрифты читабельны
- [ ] Spacing уменьшен

---

## 🎨 РЕФЕРЕНСЫ

### Навигация:
- **Stripe Docs:** https://stripe.com/docs
  - Sidebar с якорями
  - Minimal navbar
  - Breadcrumbs

- **Notion Help:** https://www.notion.so/help
  - One page scroll
  - Clean structure

### UI Паттерны:
- **Linear Docs:** https://linear.app/docs
  - Premium design
  - Smooth animations

- **Tailwind CSS Docs:** https://tailwindcss.com/docs
  - Perfect sidebar navigation
  - Active state highlighting

---

## 📦 ТЕХНИЧЕСКИЙ СТЕК

**Обязательно:**
- React 18+
- CSS (или Tailwind CSS)
- lucide-react (иконки)

**Опционально:**
- Framer Motion (для анимаций)
- Intro.js (для guided tour)
- React Router (если нужны отдельные страницы)

---

## 🔄 ПРОЦЕСС РАЗРАБОТКИ

1. **Начни с Фазы 1** (критические компоненты)
2. **Тестируй после каждого компонента**
3. **Проверяй на разных разрешениях**
4. **Переходи к Фазе 2** только после завершения Фазы 1
5. **Фаза 3 опциональна** - делай, если есть время

---

## 🚨 ВАЖНЫЕ НАПОМИНАНИЯ ДЛЯ ИИ-РАЗРАБОТЧИКА

1. **Каждая секция = отдельный компонент** (не делай всё в одном файле)
2. **Каждая секция имеет `id`** (для якорей)
3. **Smooth scroll обязателен** (не резкий переход)
4. **localStorage для чек-листа** (не терять данные)
5. **Mobile-first** (сначала проверяй на мобилке)
6. **Анимации плавные** (не дёргаются)
7. **Цвета из CSS Variables** (не хардкодить)
8. **Комментарии в коде** (объясняй, что делаешь)

---

## 📝 ИТОГОВЫЙ ЧЕКЛИСТ ПЕРЕД СДАЧЕЙ

- [ ] Навбар работает и sticky
- [ ] Sidebar с якорями работает
- [ ] Активная секция подсвечена
- [ ] Smooth scroll работает
- [ ] Кнопка "наверх" работает
- [ ] Прогресс-бар обновляется
- [ ] Табов нет (one page scroll)
- [ ] Все секции последовательны
- [ ] Чек-лист сохраняется
- [ ] Hero с градиентом
- [ ] Карточки с hover
- [ ] Мобильная версия работает
- [ ] Нет консольных ошибок
- [ ] Lighthouse Score > 85

---

**КОНЕЦ ТЕХНИЧЕСКОГО ЗАДАНИЯ**

Это ТЗ содержит ВСЁ необходимое для ИИ-разработчика. Если что-то непонятно - начинай с Фазы 1, шаг за шагом. Удачи! 🚀
